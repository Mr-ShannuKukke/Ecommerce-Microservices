spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/user_db?createDatabaseIfNotExist=true
    username: root
    password: Shannu@sql123
    driver-class-name: com.mysql.cj.jdbc.Driver # CMCJD Mnemonic to remember
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8001

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: localhost
    prefer-ip-address: false
    instance-id: user-service:8001

# JWT Authentication

jwt:
  secret: MySecretKeyForJWTTokenGenerationAndValidation2026ECommerceApplication
  expiration: 86400000  # 24 hours in milliseconds

